{
  "name": "piccatso-oauth-backend",
  "version": "1.0.0",
  "description": "OAuth backend for Piccatso Shopify app",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
